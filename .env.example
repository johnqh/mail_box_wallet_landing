# ============================================================================
# Signic ID Landing Page Environment Configuration
# ============================================================================
# Copy this file to .env.local and add your actual values
# DO NOT commit .env.local to version control

# ============================================================================
# BRANDING CONFIGURATION
# ============================================================================

# Application Name
# The display name shown in the UI, page titles, and metadata
VITE_APP_NAME=Signic ID

# Application Domain
# The primary domain for the application (e.g., signic.id)
# Used for: branding, metadata, structured data
VITE_APP_DOMAIN=signic.id

# Company Name
# The parent company name used for contact emails and legal references
VITE_COMPANY_NAME=Sudobility
